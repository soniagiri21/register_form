// Define the name of the MongoDB database to be used by the application
export const DB_NAME = "todolist";

// This constant stores the name of the MongoDB database that the application will interact with.
// It's exported so that it can be used in other parts of the application, such as the database connection configuration.
